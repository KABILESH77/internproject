
  # VibeMatch Intern UI Design

  This is a code bundle for VibeMatch Intern UI Design. The original project is available at https://www.figma.com/design/AZ95XakeTvYnVa5uDmu1Ki/VibeMatch-Intern-UI-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  